# orchestrator-core — DECISIONS (reconciliation table)

One row per rule where the source skills disagreed, or where a later operator instruction overrides older skill text. **This table governs wherever `SKILL.md` and an older orchestration skill disagree.** The originals (`watch-protocol`, `glados-loop`, `agent-liveness`, `cost-proportional-orchestration`, `subagents`, and `prompts/glados.md` before this change) are NOT edited — the "supersedes" column marks the exact older text that is now superseded in place. GLaDOS reviews this table before merge; rows flagged `NEEDS GLADOS CONFIRMATION` are the ones where the evidence trail is incomplete or points two ways.

Conventions: quotes are verbatim from the superseded text; `bd:` = a `bd memories` key; dates are the operator instruction's date.

| ID | Rule (one sentence) | Supersedes (skill §, quoted) | Source (bead / date / operator directive) | Note |
|---|---|---|---|---|
| DECISION-1 | Stuck recovery is classification-driven: a REAL HANG (tool call silent >10 min, no thinking indicator, no error banner, no frozen-counter proof) is surfaced to the operator via doorbell + terminal — never fire keys blind; an API-error silent-drop (banner visible, idle prompt) gets the BEADS dispatch re-sent; and "substantive pane content unchanged across 2 consecutive ticks" is a stall SIGNAL that forces deep-peek + classification, not an auto-interrupt. | `cost-proportional-orchestration` §3: *"if the substantive pane content (not just the timer) is unchanged across two consecutive checks, treat it as a probable hang and interrupt — don't wait for a third confirmation"* and §5 item 5 *"interrupted yet, or still being read as 'probably fine'?"* — superseded as an auto-interrupt; retained as the signal that triggers classification. `watch-protocol` §2 "silent-drop" row is narrowed to the frozen-counter evidence (see DECISION-1b), not any quiet pane. | `bd: agent-liveness-discipline-operator-directed-2026-05-23` (*"re-send BEADS message on API-error symptoms … ask operator for Ctrl+C on real hangs, NEVER re-ping agents who are deliberately paused"*); `agent-liveness` §3 REAL HANG row + §4 decision tree (*"REAL HANG → surface to operator; don't fire keys blind"*). `cost-proportional` §3 is GLaDOS's own post-mortem banked 2026-07-27 at the operator's prompt, not an operator directive on the interrupt mechanism. | **NEEDS GLADOS CONFIRMATION.** The brief expected a later operator instruction on stall interruption supporting surface-over-interrupt. The only later operator instruction on stall interruption found in the skills or the memory bank is the 2026-05-26 one in `watch-protocol` §2 (*"when someone is wrangling like that the best way is to cntr+c in the terminal THEN re dispatch or else it will keep wrangling"*), which points the OTHER way for the frozen-counter pattern — carried as DECISION-1b. If GLaDOS knows of a later instruction (post-2026-05-26) that withdraws the C-c procedure, DECISION-1b collapses into DECISION-1's surface path. |
| DECISION-1b | The one GLaDOS-fired interrupt: when a thinking indicator's `↑NNNk tokens` / cost / context% are identical across 2+ consecutive ticks while only the timer advances (or an `API Error … Rate limited` banner sits on a locked turn), fire `tmux send-keys -t <agent> C-c`, then re-dispatch via BEADS with a state recap, and verify recovery on the next sweep. | `agent-liveness` §3 REAL HANG row *"Operator-judgment territory. Surface for Ctrl+C + re-dispatch"* and §6 step 3 — **only** as applied to the frozen-counter / locked-turn pattern (agent-liveness's REAL HANG cell is defined as *no* thinking indicator, so the two rows describe different states; the contradiction was in the generalisations, not the cells). `agent-liveness` §6 *"Re-send the same BEADS message"* is insufficient alone for a locked turn. | `watch-protocol` §2 silent-drop row: operator-taught 2026-05-26 (Peppy on #431, `Wrangling 21m9s`, `↑655 tokens` frozen); 2026-05-24 Sage/Izzy/Atlas (BEADS-only recovery *"sometimes worked because rate-limit clears were soft"*). Post-dates the 2026-05-23 directive. | **NEEDS GLADOS CONFIRMATION** (same trail as DECISION-1). Rule as written is evidence-gated: no frozen-counter proof → DECISION-1 surface path. |
| DECISION-2 | `/compact` is a unilateral orchestrator decision fired via `tmux send-keys -t <agent> C-u '/compact' Enter` at **≥ 60% context** for any specialist — no pre-message, no choice, no ack — confirmed as "/compacted <agent> at NN%"; never while that agent has a live subagent (wait, then compact immediately); prefer `/compact` over `/clear`. | `watch-protocol` §2: *">60-65% context with any precision-critical work OR >70% regardless"*; `glados-loop` §5 item 6: same figures; `agent-liveness` §2 Signal B: *"% over 70 → see watch-protocol §2 trigger; ping for /clear OR fire it yourself"* (no "ping for /clear"; threshold 60; prefer /compact); `agent-liveness` §5: *"Firing /clear when an agent is over the context threshold and asking for it"* (agents don't ask; /compact preferred). Not edited but also superseded on the figure: `specialist-delegation` §7 *"at ~60–65% … (or ~70% regardless)"*. | Operator quote 2026-05-25 (*"can we make the agents stop from blocking themselves asking for compact? this is an orquestrator decision not a specialist decision"*); `bd: glados-compact-at-60-proactive` — OPERATOR DIRECTIVE 2026-06-26 (*"please dont let agents get into this level of context, once it hits 60% is always good to compact"*), which itself states *"Supersedes the 65% figure in watch-protocol §2 and glados-loop §5"* and adds the live-subagent exception (Izzy 98% on 38wja). | Later directive (2026-06-26) wins over 2026-05-25's figures; both agree on "unilateral". `specialist-delegation` §7 is outside this change's scope — flag for a follow-up edit. |
| DECISION-3 | Bead creation is GLaDOS-only and operator-acknowledged first — no exceptions, including P0s; discovered follow-ups, missing-surface beads and review tasks are batched to the operator for ack, then filed with the project label in the same turn. | `prompts/glados.md` BEADS section: *"Always create BEADS tasks for work you delegate to specialists"* (now: after operator ack, per `beads` §0). `watch-protocol` §2: *"An agent files a bead via MCP create_task … Apply the project label yourself"* (specialists no longer file; correct the agent, then relabel if a bead already exists) and *"An agent reports a discovered follow-up → File it as a P3 … bead"* (now: propose for ack, then file). `watch-protocol` §6.A: *"Missing surface = file the bead now"* / *"file the bead immediately"* (now: propose for ack, or edit the spec). | `beads` §0 operator directive 2026-07-29 (operator-confirmed no P0 exception; 307-bead noise incident). | The label rule (`watch-protocol` §4 *"Apply the label in the same turn you file the bead"*) stands unchanged. |
| DECISION-4 | Reassigning work between specialists requires operator approval; a stuck or throttled specialist gets guidance, an unstall, or a surfaced recommendation — never a quiet move. | `prompts/glados.md` Operating Principle 10: *"If a specialist is stuck, provide guidance or reassign the task"* (now: *"…provide guidance or unstall them per orchestrator-core §4; reassignment is an operator call"*). `agent-liveness` §6 step 3: *"Surface to operator OR delegate the work to a different specialist who's not throttled"* (the "OR delegate" branch is a reassignment → surface only). | `watch-protocol` §3: *"Reassigning work between specialists — if Vance is stuck on a frontend task, don't quietly move it to Rex without asking."* | Consistent with `agent-liveness` §5's own "strategic decisions stay with the operator" list. |
| DECISION-5 | Pressing Enter on a command the agent itself typed (slash command, inbox-read, agent-typed prompt matching stated intent) is permitted pane housekeeping, not a "blind key"; replace-and-fire (`C-u '/compact' Enter`) when the typed command is wrong; ping the operator first only for destructive-shaped text you don't recognise. | Nothing superseded — this row exists so DECISION-1's *"don't fire keys blind"* is not read as forbidding Enter. Harmonises `watch-protocol` §2 (*"Fire Enter immediately"*), §7 gate 2, `agent-liveness` §3 TYPED-BUT-UNSENT row, §4, §5 (*"if it's a keystroke decision, you fire the keystroke"*). | `agent-liveness` §4 validated live 2026-05-23; operator 2026-05-23 (*"you are the master orchestrator you should be more witty about this"*); 2026-05-24 incident (5 agents stalled for hours on unsent keystrokes). | Safety preconditions (deep-peek, classify, no indicator, no operator mid-composition text, re-peek ≤10 s) still apply every time. |
| DECISION-6 | Message delivery is Comms v2 (hub push + replay on reconnect); the `cat '/tmp/aperture-msg-…'` examples and "the poller delivers it again" wording in the originals are legacy mechanism, and the rules are read generically ("inbox-read command", "resend via `send_message`"). | `watch-protocol` §1/§2/§7 examples `❯ cat '/tmp/aperture-msg-...'`; `agent-liveness` §6 *"The poller delivers it again"*; `prompts/glados.md` Communication: *"The poller delivers unread messages every 5 seconds until acknowledged"* (section removed → `communicate`). | `prompts/glados.md` Inbox Monitor (Comms v2, incident 2026-07-19): *"This replaces the old poller-injected `cat /tmp/aperture-msg-*` delivery."* | Examples kept verbatim in `references/` as history; no behavioural change beyond wording. |
| DECISION-7 | Sweep order is `get_presence` → pane (`tail -30`) → bead state → PR state: presence answers online/busy/idle/offline cheaply; the pane remains ground truth for stuck-vs-working-vs-waiting. | `watch-protocol` §1: *"Read pane activity FIRST"* (now second, after presence) — the pane-is-ground-truth rule is unchanged. | `prompts/glados.md` Inbox Monitor edit 2026-09-06 (aperture-trgpo): *"`get_presence` … is your primary liveness signal before any pane-peek."* | Ordering only; `offline` per presence does not excuse skipping the pane for an agent with a claimed bead. |
| DECISION-8 | Timing floors: in_progress with no thinking indicator > 30 min → deep-peek then ping with context; post-`/clear` empty prompt > 15 min, or a dispatched bead unclaimed > 15 min → cold-start re-dispatch / ping. | `glados-loop` §5 item 3: *"idle owner who hasn't moved in >15-30 min"* (resolved to 30 unless post-/clear); `agent-liveness` §2 Signal A: *"idle prompt > 30 min on a claimed in-progress task → almost certainly stuck or waiting"* (kept). | `watch-protocol` §2 rows (30 min in_progress; 15 min post-/clear; 15 min unclaimed). | Harmonisation, no operator directive. |
| DECISION-9 | Deep-peek floor is `tail -30`; use `-40`+ (`-S -40 \| tail -40`) when in doubt. | None — harmonises `agent-liveness` §1 (*"-30 floor … Bump to -40 or more when in doubt"*), `watch-protocol` §2 (*"-S -40"*) and `bd: agent-liveness-discipline-operator-directed-2026-05-23` (*"tail -40, not -3"*). | 2026-05-23 (Rex `tail -10` mis-classification). | Harmonisation. |
| DECISION-10 | "Idle/clean" is never a self-certifying stop condition: before any stop-loop decision run the mechanical gate — (1) `bd list --status=in_progress` for every named agent, any hit = do not stop; (2) last inter-specialist review verdict must read PASS/resolved, not HOLD/FAIL/blocked; (3) prefer loosening to killing when quiet-but-not-terminal. | `glados-loop` §2 STOP CONDITIONS as originally written: *"All in-flight epics closed AND all in-flight specialists idle/clean"* read as a snapshot of pane activity. | Operator escalation 2026-07-31 (*"repeated negligence... you lack grit of getting things done"*), already patched in place in `glados-loop` §2 and `watch-protocol` §2 (verdict row). | Listed so the override is named, not silent. |
| DECISION-11 | Specialists do not self-authorise scope (new investigation tracks, tooling/harnesses, "while I'm at it" hardening, non-trivial `bd ready` self-claims) — they ask GLaDOS first; GLaDOS authorises directly only what is genuinely small and brings anything non-trivial to the operator BEFORE authorising; identical gate for Codex/GPT-backed specialists. | Any blanket "self-claim from `bd ready`" autonomy in older text (`cost-proportional` §2c: *"Self-claiming from bd ready is no longer blanket-autonomous"*). `prompts/glados.md` Proactivity ("claim it and begin work immediately") applies to GLaDOS herself and is unchanged; specialist prompts are out of this change's scope. | Operator directive 2026-07-28 (fwuzg incident: *"enough of this madness"*), `cost-proportional-orchestration` §2c. | Interacts with DECISION-3: authorised small extra work lives in the existing bead's notes; anything needing a new bead goes through the §0 ack. |
| DECISION-12 | Loop cadence and agent count are sized to the serial-engineer anchor (< 2 h: 1 agent, no standing loop; ~half day: 2–3 agents, loop only if unattended at ≥ 20–30 min; multi-day parallel: full fan-out, 10 min only while the operator actively waits) and re-sized at every scope pivot. | `glados-loop` §3: *"10m — Default for active session — multiple specialists working, mid-epic"* (now conditional on the anchor table); `glados-loop` §1 *"Start a loop when … 2+ specialists actively claimed in-progress beads"* (still a trigger, no longer sufficient on its own for a tight loop). | `cost-proportional-orchestration` §2a/§2b — EuNeném catalog epic `aperture-ckru9` 2026-07-27, operator flagged usage burn twice (47%→28%). | Later (2026-07-27) operator-flagged discipline wins over the 2026-05 interval table. |

## Cross-reference corrections (no rule change)

- `watch-protocol` §2 and `glados-loop` §5 cite *"`specialist-delegation` §8 for the full discipline"* on /compact; the context-budget section is `specialist-delegation` **§7** (§8 is Parallel Tracks). `watch-protocol` §2's *"`specialist-delegation` §9 (parallel tracks)"* is likewise **§8**. `orchestrator-core` cites the correct sections.
- `agent-liveness` §7 cites *"`subagents §11`"* for fault isolation — correct; skeleton-first is `subagents` §12 / `cost-proportional` §2d.

## Rules carried unchanged but worth naming (no conflict found)

- Verdict handling: a HOLD/FAIL/blocked verdict is open work until the next actor is dispatched (operator escalation 2026-07-31, `watch-protocol` §2).
- Four-layer completeness / no "feature live" doorbell without a user-surface walk (lz9y 2026-05-23, `watch-protocol` §5/§6).
- Deferred-to-`/clear` ≠ self-resuming (Rex y18h 2026-05-24, `watch-protocol` §2/§4).
- `prompts/glados.md` Operating Principle 13 (every implementation task gets an Izzy review task) stands; the review bead is filed under the DECISION-3 ack, batched with the epic's beads.
